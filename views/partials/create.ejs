<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title></title>
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css" integrity="sha384-ggOyR0iXCbMQv3Xipma34MD+dH/1fQ784/j6cY/iJTQUOhcWr7x9JvoRxT2MZw1T" crossorigin="anonymous">
    <link rel="stylesheet" href="../../public/stylesheets/form.css">
  </head>
  <body>

    <form method='post' action='<%=recruiter[0].id%>'>
      <div class="form-group">
        <label for="testName">Test Name</label>
        <input type="text" class="form-control" name='name' id="testName">
      </div>
      <div class="form-group">
        <label for="prompt">Prompt / Test Description</label>
        <textarea class="form-control" id="prompt" name='prompt' rows="3"></textarea>
      </div>
      <div class="form-group">
        <label for="timeLimit">Time Limit (in minutes)</label>
        <input type="number" class="form-control" name='timeLimit' id="timeLimit" min="1" oninput="checkTimeLimit()" data-toggle="tooltip" title="If you set a time limit, you cannot select the Belbin test.">
      </div>
      <div class="form-group">
        <label>Belbin Test</label>
        <div class="custom-control custom-switch">
          <input type="checkbox" class="custom-control-input" id="belbinSwitch" name="belbinSwitch" onclick="checkBelbinSwitch()" data-toggle="tooltip" title="If you select the Belbin test, you cannot set a time limit.">
          <label class="custom-control-label" for="belbinSwitch"></label>
        </div>
      </div>
      
      <button type="submit" class="btn btn-primary">Submit</button>
    </form>

    <script>
      function checkTimeLimit() {
        var timeLimit = document.getElementById('timeLimit').value;
        var belbinSwitch = document.getElementById('belbinSwitch');
        if (timeLimit) {
          belbinSwitch.disabled = true;
        } else {
          belbinSwitch.disabled = false;
        }
      }
      
      function checkBelbinSwitch() {
        var belbinSwitch = document.getElementById('belbinSwitch');
        var timeLimit = document.getElementById('timeLimit');
        if (belbinSwitch.checked) {
          timeLimit.disabled = true;
        } else {
          timeLimit.disabled = false;
        }
      }
      </script>

  </body>
</html>
